const slider=document.querySelector('.hero');if(slider){let down=false,startX,scrollLeft,startY,scrollTop;slider.addEventListener('mousedown',e=>{down=true;startX=e.pageX-slider.offsetLeft;startY=e.pageY-slider.offsetTop;scrollLeft=slider.scrollLeft;scrollTop=slider.scrollTop});slider.addEventListener('mouseleave',()=>down=false);slider.addEventListener('mouseup',()=>down=false);slider.addEventListener('mousemove',e=>{if(!down)return;e.preventDefault();const x=e.pageX-slider.offsetLeft;const y=e.pageY-slider.offsetTop;slider.scrollLeft=scrollLeft-(x-startX)*1.4;slider.scrollTop=scrollTop-(y-startY)*1.4})}
